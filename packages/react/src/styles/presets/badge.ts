export const className = 'ppi-style-badge'
export const css = `
.ppi-style-badge {
  border-radius: 999px;
  padding: 7px 16px;
  background: color-mix(in srgb, currentColor 10%, transparent);
  border: 1px solid color-mix(in srgb, currentColor 25%, transparent);
  gap: 6px;
  font-size: 12px;
  font-weight: 500;
  color: currentColor;
  letter-spacing: 0.01em;
}
`
