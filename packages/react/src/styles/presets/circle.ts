export const className = 'ppi-style-circle'
export const css = `
.ppi-style-circle {
  border-radius: 50%;
  padding: 14px;
  background: color-mix(in srgb, currentColor 8%, transparent);
  border: 1.5px solid color-mix(in srgb, currentColor 22%, transparent);
}
`
